import { siteConfig } from "@/config/site";

export function SiteFooter() {
  return (
    <footer className="px-12 bg-primary dark:bg-background border-t">
      <div className="flex h-[64px] items-center">
      </div>
    </footer>
  );
}
